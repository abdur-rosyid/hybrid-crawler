# crawler_controllers

Launches unified controllers:

- one cable force PID controller on `/cable/...`
- one front soft force PID + pressure combiner + pump PID on `/soft/front/...`
- one rear soft force PID + pressure combiner + pump PID on `/soft/rear/...`
